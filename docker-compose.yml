version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: luxor-db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: bidding_system
    ports:
      - "5432:5432"